from django.apps import AppConfig


class SchedulingConfig(AppConfig):
    """Agendamento e realização da coleta (docs 03 §4, 05, 06)."""

    name = "apps.scheduling"
    verbose_name = "Agendamento"
