from django.apps import AppConfig


class CatalogConfig(AppConfig):
    """Catálogo de exames e preços por laboratório (decisão G-01)."""

    name = "apps.catalog"
    verbose_name = "Catálogo de exames"
