from django.apps import AppConfig


class OrganizationsConfig(AppConfig):
    """Laboratórios, revendedores e farmácias (doc 03 §4, doc 06)."""

    name = "apps.organizations"
    verbose_name = "Organizações"
