from django.apps import AppConfig


class CollectionPointsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.collection_points"
    verbose_name = "Locais de coleta"
