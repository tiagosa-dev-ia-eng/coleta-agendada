from django.apps import AppConfig


class RequestsConfig(AppConfig):
    """Solicitações de coleta e máquina de estados (docs 03 §4, 05, 06)."""

    name = "apps.requests"
    verbose_name = "Solicitações"
