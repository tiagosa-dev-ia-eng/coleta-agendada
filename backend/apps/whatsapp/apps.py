from django.apps import AppConfig


class WhatsappConfig(AppConfig):
    """WhatsApp + webhook próprio (doc 08) — provider simulator até G-05."""

    name = "apps.whatsapp"
    verbose_name = "WhatsApp"
